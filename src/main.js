import React from 'react'
import ReactDOM from 'react-dom'
//封装一个评论项组件，此组件由于不需要自己的私有数据，所以直接定义为无状态组件
import Counter from 'redux_test.js'

ReactDOM.render(<Counter>



</Counter>, document.getElementById('app')
    )